{
  "enabled": true,
  "patterns": {
    "keywords": [
      { "value": "application-local.yaml", "category": "LOCAL_CONFIG" }
    ],
    "regex": [
      { "pattern": "(ghp|gho|ghu|ghs|ghr)_[A-Za-z0-9]{36,40}", "category": "GITHUB_TOKEN" },
      { "pattern": "(?i)(JWT_SECRET|HERMES_API_KEY)\\s*[=:]\\s*\\S+", "category": "ENV_SECRET" }
    ],
    "builtin": []
  }
}
