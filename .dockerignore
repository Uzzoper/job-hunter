target/
.git/
.gitignore
.gitattributes
.env
# Local profile config carries secrets — never let it reach the build context
# (mvn package would embed it in app.jar). **/ form: bare names only match at
# the context root, and this file lives under src/main/resources/.
**/application-local.yaml
# Runtime artifacts that appear at repo root once the app runs
data/
# Personal file (real resume)
curriculo-ats.pdf
*.md
.DS_Store
node_modules/
linkedin-scraper/
cli/
uploads/
spring-boot.log
.idea/
.vscode/
.sisyphus/
.m2/
org/
scratch_debug.java
