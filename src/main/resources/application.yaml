spring:
  application:
    name: job-hunter

  datasource:
    url: jdbc:postgresql://localhost:5433/jobhunter
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET}
  expiration-hours: 24

ai:
  openrouter:
    base-url: https://openrouter.ai/api/v1
    api-key: ${OPENROUTER_API_KEY}
    model: minimax/minimax-m2.5:free
    timeout-seconds: 30

scraper:
  gupy:
    base-url: https://portal.api.gupy.io
    keywords: desenvolvedor,developer,engenheiro de software,software engineer,fullstack,full stack,backend,front-end,frontend,ti,tecnologia
    exclude-keywords: sênior,senior,sr,sr.,especialista,lead,coordenador,manager
    locations: ""
    limit: 100
    timeout-seconds: 10
  infojobs:
    enabled: true
    base-url: https://www.infojobs.com.br
    keywords: desenvolvedor junior,desenvolvedor júnior,analista desenvolvedor junior,programador junior,java junior,backend junior,desenvolvedor jr,programador jr,dev jr,java jr,backend jr,frontend jr,fullstack jr,c# jr,python jr,.net jr
    exclude-keywords: senior,sênior,sr,sr.,lead,especialista,bdr,desenvolvedor de negócios,desenvolvimento de negócios,business development
    locations: ""
    max-pages: 3
    max-age-days: 30
    timeout-seconds: 10
    delay-millis: 2000

scheduler:
  fetch-jobs:
    cron: "0 0 */6 * * *"
